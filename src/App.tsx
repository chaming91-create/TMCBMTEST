import { useState } from 'react';import { Activity,BarChart3,Database,FileClock,FileSpreadsheet,Gauge,History,Menu,PlusCircle,Settings,ShieldAlert,TrainFront } from 'lucide-react';import Dashboard from './components/Dashboard';import TmStatusTable from './components/TmStatusTable';import ReplacementHistoryTable from './components/ReplacementHistoryTable';import ReplacementInputForm from './components/ReplacementInputForm';import SerialDetailView from './components/SerialDetailView';import RiskAnalysis from './components/RiskAnalysis';import ExcelImportExport from './components/ExcelImportExport';import ValidationResults from './components/ValidationResults';import SettingsPage from './components/SettingsPage';import LoginGate from './components/LoginGate';import { useApp } from './context/AppContext';
const tabs=[['dashboard','대시보드',BarChart3],['tm','TM 현황',TrainFront],['history','교체현황',History],['input','신규 교체 입력',PlusCircle],['serial','Serial No. 상세이력',FileClock],['risk','리스크 분석',Gauge],['excel','엑셀 업로드/다운로드',FileSpreadsheet],['validation','오류/검증 결과',ShieldAlert],['settings','설정',Settings]] as const;
export default function App(){const [tab,setTab]=useState('dashboard'),[open,setOpen]=useState(true);const {issues}=useApp();const page={dashboard:<Dashboard/>,tm:<TmStatusTable onSerial={()=>setTab('serial')}/>,history:<ReplacementHistoryTable/>,input:<ReplacementInputForm onSaved={()=>setTab('tm')}/>,serial:<SerialDetailView/>,risk:<RiskAnalysis/>,excel:<ExcelImportExport/>,validation:<ValidationResults/>,settings:<SettingsPage/>}[tab];return <LoginGate><div className="app-shell"><aside className={open?'sidebar':'sidebar collapsed'}><div className="brand"><div className="brand-mark"><Activity/></div><div><strong>TM-CBM</strong><span>견인전동기 통합관리</span></div></div><nav>{tabs.map(([id,label,Icon])=><button key={id} className={tab===id?'active':''} onClick={()=>setTab(id)} title={label}><Icon/><span>{label}</span>{id==='validation'&&issues.length>0?<b>{issues.length}</b>:null}</button>)}</nav><div className="db-status"><Database/><span>Firebase 연동 준비</span></div></aside><main><header><button className="icon-button" onClick={()=>setOpen(v=>!v)}><Menu/></button><div><p>철도차량 상태기반 유지보수 플랫폼</p><h1>{tabs.find(x=>x[0]===tab)?.[1]}</h1></div><div className="header-pill"><span></span>시스템 정상</div></header><div className="page-content">{page}</div></main></div></LoginGate>}
