import { useMemo,useState } from 'react';import { Download,RotateCcw,Search } from 'lucide-react';import { useApp } from '../context/AppContext';import { downloadWorkbook,exportTm } from '../lib/excelParser';import EmptyState from './EmptyState';import RiskBadge from './RiskBadge';
export default function TmStatusTable({onSerial}:{onSerial:()=>void}){const {tms,risks}=useApp();const [q,setQ]=useState(''),[train,setTrain]=useState(''),[status,setStatus]=useState(''),[spare,setSpare]=useState(false),[historyOnly,setHistoryOnly]=useState(false);const rows=useMemo(()=>tms.filter(tm=>{const risk=risks.find(x=>x.serialNo===tm.serialNo);const searchable=`${tm.tmId||''} ${tm.serialNo} ${tm.currentTrain} ${tm.currentCar} ${tm.currentUnit||tm.currentPosition} ${tm.manufacturer} ${risk?.riskGrade||''}`.toLowerCase();return(!q||searchable.includes(q.toLowerCase()))&&(!train||tm.currentTrain===train)&&(!status||tm.currentStatus===status)&&(!spare||tm.isSpare)&&(historyOnly?tm.sourceType==='history_only':tm.sourceType!=='history_only')}),[tms,risks,q,train,status,spare,historyOnly]);const reset=()=>{setQ('');setTrain('');setStatus('');setSpare(false);setHistoryOnly(false)};return <div className="stack"><section className="filter-panel"><div className="search"><Search/><input placeholder="TM_ID·시리얼·편성·차호·위수 검색" value={q} onChange={e=>setQ(e.target.value)}/></div><select value={train} onChange={e=>setTrain(e.target.value)}><option value="">전체 편성</option>{[...new Set(tms.filter(x=>x.sourceType!=='history_only').map(x=>x.currentTrain).filter(Boolean))].sort().map(x=><option key={x}>{x}</option>)}</select><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">전체 상태</option>{[...new Set(tms.map(x=>x.currentStatus).filter(Boolean))].sort().map(x=><option key={x}>{x}</option>)}</select><label className="check"><input type="checkbox" checked={spare} onChange={e=>setSpare(e.target.checked)}/>예비품만</label><label className="check"><input type="checkbox" checked={historyOnly} onChange={e=>setHistoryOnly(e.target.checked)}/>이력만 존재</label><button onClick={reset}><RotateCcw/>초기화</button><button className="primary" onClick={()=>downloadWorkbook('TM_현황',[{name:'TM 현황',data:exportTm(rows,risks)}])}><Download/>엑셀 다운로드</button></section><section className="panel table-panel"><div className="panel-heading"><h3>TM 현황 <span>{rows.length.toLocaleString()}대</span></h3></div>{!rows.length?<EmptyState/>:<div className="table-wrap data-table tm-table"><table><thead><tr>{['TM_ID','확인일자','편성','차호','위수','시리얼번호','제작년도','제조사','사용연수','현재상태','최근 교체/취부일자','위치정보 출처','Risk_Score','위험등급'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{rows.map(tm=>{const risk=risks.find(x=>x.serialNo===tm.serialNo);return <tr key={tm.serialNo} className={risk?.riskGrade==='위험'?'danger-row':''}><td>{tm.tmId||'-'}</td><td>{tm.confirmedAt||'-'}</td><td>{tm.currentTrain||'-'}</td><td>{tm.currentCar||'-'}</td><td><strong>{tm.currentUnit||tm.currentPosition||'-'}</strong></td><td><button className="link" onClick={()=>{sessionStorage.setItem('selectedSerial',tm.serialNo);onSerial()}}>{tm.serialNo}</button></td><td>{tm.manufactureYear??'-'}</td><td>{tm.manufacturer||'-'}</td><td>{tm.ageYear}</td><td>{tm.currentStatus}</td><td>{tm.installDate||'-'}</td><td>{tm.locationSource||'현황파일'}</td><td><strong>{risk?.riskScore??0}</strong></td><td>{risk&&<RiskBadge grade={risk.riskGrade}/>}</td></tr>})}</tbody></table></div>}</section></div>}
