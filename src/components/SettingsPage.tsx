import { useState } from 'react';import { Calculator,Save,ShieldAlert } from 'lucide-react';import { useApp } from '../context/AppContext';
export default function SettingsPage(){const {settings,severities,updateSettings}=useApp();const [s,setS]=useState(structuredClone(settings)),[sm,setSm]=useState(structuredClone(severities)),[saved,setSaved]=useState(false);const n=(path:string,v:number)=>{const x=structuredClone(s) as any;const parts=path.split('.');if(parts.length===1)x[path]=v;else x[parts[0]][parts[1]]=v;setS(x)};const weight=s.weights.A+s.weights.F+s.weights.T+s.weights.M;return <div className="settings-layout"><div className="stack"><section className="panel"><h3><Calculator/>리스크 계산 기본값</h3><div className="form-grid settings-grid">{[['mkbf','MKBF 값 (km/건)'],['referenceYear','기준년도'],['weibullBeta','Weibull beta'],['weibullEta','Weibull eta'],['alpha','EB 보정 alpha']].map(([k,l])=><label key={k}>{l}<input type="number" step="any" value={(s as any)[k]} onChange={e=>n(k,Number(e.target.value))}/></label>)}</div></section><section className="panel"><h3>리스크 가중치</h3><div className="form-grid settings-grid">{Object.entries(s.weights).map(([k,v])=><label key={k}>{k} 점수 가중치<input type="number" min="0" max="1" step="0.01" value={v} onChange={e=>n(`weights.${k}`,Number(e.target.value))}/></label>)}</div><p className={Math.abs(weight-1)>.001?'form-error':'weight-ok'}>가중치 합계: {weight.toFixed(2)} {Math.abs(weight-1)>.001?'(합계는 1이어야 합니다.)':'(정상)'}</p></section><section className="panel"><h3>위험등급 기준값</h3><div className="form-grid settings-grid">{[['thresholds.observation','관찰 시작'],['thresholds.caution','주의 시작'],['thresholds.danger','위험 시작']].map(([k,l])=><label key={k}>{l}<input type="number" min="0" max="100" value={(s.thresholds as any)[k.split('.')[1]]} onChange={e=>n(k,Number(e.target.value))}/></label>)}</div></section><section className="panel table-panel"><div className="panel-heading"><h3>고장유형별 심각도 점수</h3><button onClick={()=>setSm(x=>[...x,{failureType:'신규 고장유형',severityClass:'C',severityScore:10,description:'',isActive:true}])}>+ 유형 추가</button></div><div className="table-wrap"><table><thead><tr><th>고장유형</th><th>등급</th><th>점수</th><th>설명</th><th>사용</th></tr></thead><tbody>{sm.map((x,i)=><tr key={`${x.failureType}-${i}`}><td><input value={x.failureType} onChange={e=>setSm(a=>a.map((v,j)=>j===i?{...v,failureType:e.target.value}:v))}/></td><td><input value={x.severityClass} onChange={e=>setSm(a=>a.map((v,j)=>j===i?{...v,severityClass:e.target.value}:v))}/></td><td><input type="number" min="0" max="100" value={x.severityScore} onChange={e=>setSm(a=>a.map((v,j)=>j===i?{...v,severityScore:Number(e.target.value)}:v))}/></td><td><input value={x.description} onChange={e=>setSm(a=>a.map((v,j)=>j===i?{...v,description:e.target.value}:v))}/></td><td><input type="checkbox" checked={x.isActive} onChange={e=>setSm(a=>a.map((v,j)=>j===i?{...v,isActive:e.target.checked}:v))}/></td></tr>)}</tbody></table></div></section><div className="form-actions sticky-actions">{saved&&<span className="success-message">저장 및 재계산 완료</span>}<button className="primary" disabled={Math.abs(weight-1)>.001} onClick={async()=>{await updateSettings(s,sm);setSaved(true)}}><Save/>설정 저장 및 전체 재계산</button></div></div><aside className="panel danger-zone"><ShieldAlert/><h3>위험 작업</h3><p>DB 초기화는 오입력 방지를 위해 확인 문구가 필요합니다.</p><input placeholder="DB초기화 입력"/><button disabled>DB 전체 초기화</button><small>초기화 기능은 Firebase 관리자 권한을 별도 구성한 후 활성화하세요.</small></aside></div>}
