import type { ReplacementHistory } from '../types/replacement';import type { ValidationIssue,RiskSettings } from '../types/risk';import type { SeverityMaster,TmMaster } from '../types/tm';import { toSeverityMap,validateMasterRows,validateReplacementRows } from './excelParser';
export function validateData(tms:TmMaster[],history:ReplacementHistory[],sm:SeverityMaster[],_settings:RiskSettings):ValidationIssue[]{return [...validateMasterRows(tms),...validateReplacementRows(history,tms,toSeverityMap(sm))];}
