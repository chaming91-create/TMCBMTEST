import { describe,expect,it } from 'vitest';import { calculateAgeScore,calculateMkbfScore,getRiskGrade } from '../riskCalculator';import { DEFAULT_SETTINGS } from '../defaults';
describe('리스크 계산',()=>{it('MKBF 기본 점수를 계산한다',()=>expect(calculateMkbfScore(38533)).toBeCloseTo(22.86,1));it('미래 제작년도는 0점이다',()=>expect(calculateAgeScore(2030,DEFAULT_SETTINGS)).toBe(0));it('위험등급 경계를 적용한다',()=>{expect(getRiskGrade(39.99,DEFAULT_SETTINGS)).toBe('정상');expect(getRiskGrade(40,DEFAULT_SETTINGS)).toBe('관찰');expect(getRiskGrade(60,DEFAULT_SETTINGS)).toBe('주의');expect(getRiskGrade(80,DEFAULT_SETTINGS)).toBe('위험')})});
